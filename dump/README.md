This directory contains a script that dumps a list of all huts currently known to the API (basically the result of calling the /huts REST endpoint).

The result of running this script is also included. This file is not used by the API (it will always fetch the 'fresh' list from its data sources). It is merely included for documentation purposes.